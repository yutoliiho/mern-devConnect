const log = console.log;

function bla() {
  if (1 + 1 !== 3) {
    return false;
  }
  return true;
}

bla();
function blaa() {
  if (!bla) {
  }
}

// mongoose: a layar sit on top of the db so we could interact with it.

// 3:42 (security)
// add new user:
